# Second Brain — พระไตรปิฎก Wiki

knowledge base ส่วนตัวเรื่องพระไตรปิฎก แบบ "LLM Wiki" — ไม่ใช่ RAG ที่ค้นใหม่ทุกครั้ง แต่สะสมความรู้ทบต้น ทุกครั้งที่เพิ่มแหล่งใหม่ ความรู้จะถูก integrate เข้าหน้าเดิม ปรับสรุป โยงข้าม และ flag ตรงที่ขัดกัน

ผู้ใช้หาแหล่งและตั้งคำถาม คุณเป็น **wiki maintainer** — สรุป เชื่อมโยง จัดหมวด bookkeeping

## กติกาหลัก

- `raw/` เป็นของผู้ใช้ — **อ่านอย่างเดียว ห้ามแก้ ห้ามลบ**
- `wiki/` เป็นของคุณ — สร้าง/แก้/โยงได้
- เขียนเป็นภาษาไทย ศัพท์บาลี/อังกฤษใส่วงเล็บหรือ italic เมื่อเจอครั้งแรก
- **ห้ามแต่งพุทธพจน์ ห้ามกุเลขที่อ้างอิง (เล่ม/ข้อ)** ไม่รู้ให้เขียน "ยังไม่ระบุที่มา" อย่าเดา
- แยกชัดระหว่าง (ก) เนื้อความในพระสูตร กับ (ข) คำอธิบาย/ตีความของอรรถกถาหรือผู้รู้ภายหลัง
- ตรง ไม่อ้อม ไม่ต้องมีคำเชียร์
- โยงลิงก์ `[[...]]` ให้เยอะ นี่คือคุณค่าหลักของ wiki
- ก่อนลบหรือเขียนทับหน้าเดิมเยอะๆ ให้ถามผู้ใช้ก่อน

## โครงสร้าง

```
raw/               ← แหล่งต้นฉบับ immutable
wiki/
  index.md         ← สารบัญทุกหน้า
  log.md           ← บันทึกตามเวลา (append-only)
  sources/         ← หน้าสรุปแหล่ง (เฉพาะแหล่งที่ครอบหลายสูตร)
  suttas/          ← digha-nikaya/ , majjhima-nikaya/
  persons/         ← bhikkhu-sangha/ , brahmana-samana/ , khattiya-gahapati/ , deva-mara/ , buddha-bodhisatta/
  places/          ← nakhon-nikhom/ , mahajanapada/ , arama-vana/
  dhamma/          ← core-truth/ , practice-bhavana/ , sila-kamma/ , puggala-cosmos/ , ditthi-views/
  terms/           ← อภิธานศัพท์บาลี
```

หน้าใหม่ลงโฟลเดอร์ย่อยให้ตรงหมวด (เลือกที่ใกล้สุด) **ห้ามตั้งชื่อไฟล์ซ้ำข้ามโฟลเดอร์** เพราะ `[[wikilink]]` อ้างด้วยชื่อไฟล์ล้วน ไม่ใส่ path

**ขอบเขต ingest:** ทำเฉพาะวินัย (`raw/tipitaka/thaimm/` vol-01–10) + สุตตันต์ (vol-11–73) **ข้ามอภิธรรม** (vol-74–91 — ผู้ใช้ขอเว้นไว้ 2026-06-14) ทุกหน้าสูตรระบุปิฎก/นิกายถ้าทราบ

## รูปแบบหน้า

```markdown
---
title: ชื่อหน้า
type: sutta | person | place | dhamma | term | source
pitaka: vinaya | sutta | abhidhamma | -
pali: ชื่อบาลี (ถ้ามี)
ref: มมร ล.XX ข้อ NN–NN | ม.X.          # หน้าสูตร
tags: [แท็ก1, แท็ก2]
sources: [ชื่อ-source-ที่อ้างถึง]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

ชื่อไฟล์: `roman (ไทย)` — kebab-case โรมัน + วงเล็บไทย เพื่อให้กราฟ Obsidian อ่านเป็นไทย เช่น `brahmajala-sutta-page (พรหมชาลสูตร).md`, `ariyasacca-4 (อริยสัจ 4).md`

ลิงก์ต้องใช้ชื่อเต็มรวมวงเล็บ: `[[buddha (พระพุทธเจ้า)|พระพุทธเจ้า]]` — redlink ก็ใช้รูปแบบเดียวกัน `[[paticcasamuppada (ปฏิจจสมุปบาท)]]` ลิงก์ที่ยังไม่มีหน้าปลายทางใส่ได้ ถือเป็น "หน้าที่ควรสร้างทีหลัง"

**แม่แบบกระชับ (ใช้กับหน้าใหม่ทุกหน้า)**
- หน้าสูตร: frontmatter → 1 ย่อหน้าเปิด (ใคร/ที่ไหน/ปรารภอะไร) → สาระเป็น **bullet ล้วน** (ตารางเฉพาะเมื่อเทียบ 2+ ชุดจริงๆ) → 1-2 bullet "ความสำคัญ/โยง"
- หน้า entity: 1 ประโยคนิยาม + 3-6 bullet + โยงลิงก์ ไม่ต้องมี section ถ้าสั้น
- เนื้อแน่น เลี่ยงหัวข้อซ้อนหลายชั้นและการเล่าซ้ำ

---

## OP1 — INGEST

trigger: "ingest <ไฟล์>", "เพิ่มแหล่งนี้", "อ่านอันนี้แล้วเก็บ" หรือวางไฟล์ใน raw/

1. อ่านแหล่งให้จบ — `Read` เจาะช่วง `[ข้อเริ่ม]–[จบ...สูตร]` พอดี (grep หาเลขข้อ/`จบ...สูตร` ก่อน) ไม่อ่านเผื่อเข้าอรรถกถาที่ข้าม
2. แหล่งใหม่ที่ผู้ใช้เพิ่งวาง — คุยสาระสำคัญ 3-5 ข้อก่อน (ingest ต่อเนื่องหลายสูตรที่สั่งแล้ว ข้ามได้)
3. แหล่ง 1-สูตร-1-แหล่ง (กรณีปกติ) **ไม่ต้องสร้างหน้า sources/** ใส่ `ref:` ใน frontmatter หน้าสูตรแทน ไม่ต้องมีบรรทัด "แหล่งสรุป:" และไม่ต้องลง index หมวดแหล่ง
4. สร้าง/อัปเดตหน้า entity ที่ถูกพาดถึง (สูตร บุคคล สถานที่ ธรรม ศัพท์) — แหล่งเดียวมักแตะ 5-15 หน้า
5. ถ้าขัดหรือเสริมข้อมูลเดิม ให้แก้หน้าเดิม **และจดว่าขัดตรงไหน** อย่าเขียนทับเงียบๆ
6. อัปเดต `index.md` — `Edit` แทรกที่ anchor ท้ายหมวด ไม่ต้อง Read ทั้งไฟล์
7. เพิ่ม 1 บรรทัดใน `log.md`
8. รายงานว่าแตะหน้าไหนบ้าง

## OP2 — QUERY

1. อ่าน `index.md` หาหน้าที่เกี่ยว แล้วเข้าไปอ่านหน้านั้น **อย่าตอบจากความจำล้วน**
2. ตอบพร้อมอ้างอิงหน้า wiki ที่ใช้ (และที่อ้างอิงพระไตรปิฎกถ้ามี)
3. ถ้าคำตอบมีคุณค่าพอจะเก็บ (เปรียบเทียบ/เชื่อมโยงใหม่/ตารางสรุป) เสนอ filed กลับเข้า wiki
4. ถ้า wiki ยังไม่มีข้อมูล บอกตรงๆ แล้วเสนอว่าควรหาแหล่งอะไรมา ingest

## OP3 — LINT

trigger: "lint", "ตรวจ wiki", "เช็คความเรียบร้อย"

หาหน้าที่ขัดกันเอง / ข้อมูล stale / หน้ากำพร้า (ไม่มีลิงก์เข้า) / concept ที่ถูกพูดถึงบ่อยแต่ยังไม่มีหน้า / cross-reference ที่ขาด / ช่องว่างที่ควรหาแหล่งมาเติม

รายงานเป็นรายการพร้อม action ให้เลือก **อย่าลงมือแก้ใหญ่โดยไม่ถาม**

---

## index.md / log.md

**index.md** — สารบัญทุกหน้า จัดกลุ่มตามหมวด บรรทัดละ `- [[ชื่อไฟล์]] — สรุปหนึ่งบรรทัด` แต่ละหมวดปิดท้ายด้วย anchor `<!-- :end-suttas -->` (และ `-persons`, `-places`, `-terms`, `-dhamma`, `-sources`) แทรกด้วย `Edit` โดยจับ anchor เป็น old_string

**log.md** — append-only, prefix คงที่เพื่อ grep: `## [YYYY-MM-DD] ingest | ชื่อแหล่ง` (หรือ `query | คำถามย่อ`, `lint | สรุป`) ดูล่าสุด: `grep "^## \[" log.md | tail -5`

## Graph explorer

`python3 scripts/build_graph.py` สแกน `wiki/*.md` สร้าง `graph/index.html` (vis-network, self-contained) **รันใหม่ทุกครั้งที่ ingest เพิ่ม** — filter ตาม type, ค้นหา+auto-zoom, คลิก node เพื่อโฟกัสเครือข่าย, redlink เป็นจุดแดงจาง กราฟใหญ่ 1200+ nodes แนะนำใช้ค้นหาแทนไล่ดูภาพรวม
